// IAudioNotificationStatefulParams is more of a convenience to avoid using "any" in AudioNotificationStateful component
export interface IAudioNotificationStatefulParams {
    audioSrc?: string;
    isAudioMuted?: boolean;
}